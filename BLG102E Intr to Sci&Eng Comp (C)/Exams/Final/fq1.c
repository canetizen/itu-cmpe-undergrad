#include <stdio.h>
#include <string.h>
#include <stdlib.h>

// Taking argument as command line
int main(_____________________________)
{
   // Checking if number of arguments is at most 5
   if (____________________________) 
   {
      printf("Enter 1 to 5 arguments!\n");
      return __;
   }
 
   // put your additional code below
   // ...



   printf("Female count: %d, avg. female name length: %2.1f\n", ___________________________________);
   printf("Male count: %d, avg. male name length: %2.1f\n", _______________________________________);

   return 0;
}